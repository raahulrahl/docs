---
title: "Health & metrics"
description: "/health says yes. /metrics says how. Both are running before you write a line of monitoring code."
---

A process can be "running" and still broken. Postgres is down, Redis dropped the connection, the worker pool deadlocked — the agent's still binding port `:3773` and responding to `GET /health`, but the next real request fails.

That's why `/health` returns more than `200 OK` in Bindu. It exposes readiness state (can it actually take a task right now?), dependency status (storage, scheduler, LLM provider reachable?), and the boring stuff your load balancer wants — uptime, version, agent ID. `/metrics` exposes Prometheus-formatted numbers for traffic, latency, task state counts, and worker pool load.

You don't configure either. They're up the moment `bindufy()` starts the server. Point your monitoring at them and you have visibility from request one.

## How Health And Metrics Work

Bindu exposes two complementary views:

```text
/health  -> current runtime state
/metrics -> time-series operational signals
```

- `/health` answers "is this agent ready right now?"
- `/metrics` answers "how has this agent been behaving over time?"

<CardGroup cols={3}>
  <Card title="Readable" icon="heart-pulse">
    Health responses expose runtime, application, and system state in one place.
  </Card>
  <Card title="Queryable" icon="chart-line">
    Metrics can be scraped by Prometheus-compatible systems for long-term monitoring.
  </Card>
  <Card title="Operational" icon="shield-check">
    Together they help teams detect readiness issues and performance drift early.
  </Card>
</CardGroup>

---

## Health Endpoint

<Steps>
  <Step title="Check Health">
    The health endpoint answers the simplest operational question first: is the agent
    running and ready?

    <CodeGroup>
      ```bash Request
      curl http://localhost:3773/health
      ```

      ```json Response
      {
        "status": "ok",
        "ready": true,
        "uptime_seconds": 37.56,
        "version": "2026.6.7.dev65+g6742cd6eb.d20260210",
        "health": "healthy",
        "runtime": {
          "storage_backend": "PostgresStorage",
          "scheduler_backend": "RedisScheduler",
          "task_manager_running": true,
          "strict_ready": true
        },
        "application": {
          "penguin_id": "<penguin_id>",
          "agent_did": "<agent_did>"
        },
        "system": {
          "python_version": "<python_version>",
          "platform": "<platform>",
          "environment": "<environment>"
        }
      }
      ```
    </CodeGroup>

    Response fields:

    | Field | Description |
    | --- | --- |
    | `status` | Overall health status (`ok` or `degraded`) |
    | `ready` | Whether agent is ready to accept requests |
    | `uptime_seconds` | Time since agent started |
    | `version` | Bindu version number |
    | `health` | Health status (`healthy` or `degraded`) |
    | `runtime.storage_backend` | Storage backend type (e.g., `PostgresStorage`) |
    | `runtime.scheduler_backend` | Scheduler backend type (e.g., `RedisScheduler`) |
    | `runtime.task_manager_running` | Whether task manager is running |
    | `runtime.strict_ready` | Strict readiness check status |
    | `application.penguin_id` | Unique agent instance identifier |
    | `application.agent_did` | Agent's Decentralized Identifier |
    | `system.python_version` | Python version |
    | `system.platform` | Operating system platform |
    | `system.environment` | Deployment environment |
  </Step>

  <Step title="Inspect Runtime State">
    The health response is not just a liveness check. It exposes enough context to tell
    whether the agent is actually safe to receive work.

    Readiness is often a dependency question. If storage, scheduling, or the task
    manager is degraded, the agent may be present but not truly ready.
  </Step>
</Steps>

---

## Metrics Endpoint

The metrics endpoint exposes Prometheus-compatible time-series data so you can watch traffic, latency, concurrency, and active task load over time.

<CodeGroup>
  ```bash Request
  curl http://localhost:3773/metrics
  ```

  ```prometheus Response
  # HELP http_requests_total Total number of HTTP requests
  # TYPE http_requests_total counter
  http_requests_total{method="GET",endpoint="/health",status="200"} 1

  # HELP agent_tasks_active Currently active tasks
  # TYPE agent_tasks_active gauge
  agent_tasks_active{agent_id="did:bindu:..."} 0

  # HELP http_request_duration_seconds HTTP request latency
  # TYPE http_request_duration_seconds histogram
  http_request_duration_seconds_bucket{le="0.1"} 12
  http_request_duration_seconds_count 12
  ```
</CodeGroup>

Available metrics:

| Metric | Type | Description |
| --- | --- | --- |
| `http_requests_total` | counter | Total HTTP requests by method, endpoint, status |
| `http_request_duration_seconds` | histogram | Request latency |
| `agent_tasks_active` | gauge | Currently active tasks |
| `agent_tasks_completed_total` | counter | Total completed tasks by agent and status |
| `http_response_size_bytes` | summary | Response body size |
| `http_requests_in_flight` | gauge | Current requests being processed |

---

## Real-World Use Cases

<AccordionGroup>
  <Accordion title="Kubernetes or container readiness checks">
    A deployment platform can call `/health` before routing traffic to the agent,
    making sure requests only hit instances that are actually ready.

    ```bash
    curl http://localhost:3773/health
    ```
  </Accordion>

  <Accordion title="Prometheus-based monitoring">
    A monitoring stack can scrape `/metrics` continuously to track request counts,
    latency changes, and current task volume.

    ```bash
    curl http://localhost:3773/metrics
    ```
  </Accordion>

  <Accordion title="Debugging runtime dependencies">
    If an agent appears live but behaves incorrectly, the health response can reveal
    which runtime pieces are active and whether strict readiness is being met.

    ```json
    {
      "runtime": {
        "storage_backend": "PostgresStorage",
        "scheduler_backend": "RedisScheduler",
        "task_manager_running": true,
        "strict_ready": true
      }
    }
    ```
  </Accordion>
</AccordionGroup>

---

## Best Practices

<CardGroup cols={2}>
  <Card title="Monitor Readiness, Not Just Liveness" icon="heart-pulse">
    Use the full `/health` response to validate agent readiness instead of relying only
    on process existence.
  </Card>
  <Card title="Track Metrics Continuously" icon="chart-column">
    Scrape `/metrics` over time so latency, concurrency, and traffic issues are visible
    before they become outages.
  </Card>
</CardGroup>

---

## Related

- [Prometheus](https://prometheus.io/)
- [Observability](/bindu/learn/observability/overview)
- [Storage](/bindu/learn/storage/overview)

<span className="brand-quote">
  <img
    src="/logo/light.svg"
    alt="Sunflower Logo"
    width="32"
    className="clean-icon"
  />

  <span className="brand-quote-text">
    With Bindu, agent health is like a field of sunflowers —{" "}
    <span className="brand-quote-highlight">each one standing on its own</span>,
    yet easy to observe and trust.
  </span>
</span>
